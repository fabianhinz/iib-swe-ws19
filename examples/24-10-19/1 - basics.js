/* eslint-disable no-unused-vars */

// ? Deklaration
let aMutableVariable
const anImmutableVariable = ''

// ? Datentypen
const aString = 'string'
const aNumber = 2
const aBoolean = true
const anEmptyObject = {}
const aFunction = () => aNumber

// ? Literale
const stringLiteral = ''
const templateLiteral = `1 + 1 equals ${aNumber}`
const arrayLiteral = []
const objectLiteral = {}
