import { createMuiTheme, responsiveFontSizes } from '@material-ui/core'

const theme = createMuiTheme({ palette: { type: 'dark' }, overrides: {} })
export const responsiveTheme = responsiveFontSizes(theme)
//jhjjj
//blabla
//afeegege
